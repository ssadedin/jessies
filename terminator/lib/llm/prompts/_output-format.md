## How to reply

If the most useful reply is a single command the user could run at the prompt, put it on the first line in exactly this form:
COMMAND: <the command>
You may follow it with at most three short lines of explanation.

Otherwise reply in plain text, in at most about 15 short lines. Put each command on a line of its own. Don't use Markdown headings, tables or bold text.
