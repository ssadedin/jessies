## How to reply

If the single most useful reply is one command the user could run at the prompt, reply with exactly one line in this form, and nothing else:
COMMAND: <the command>

Otherwise reply in plain text, in at most about 15 short lines. Put each command on a line of its own. Don't use Markdown headings, tables or bold text.
