---
description: Used instead of the classifier when "Skip classification pass" is on. It has no scenario, so the classifier never chooses it.
status: Looking for something useful
---
{{> _system}}

{{> _about-the-user}}
---8<---
{{> _terminal}}

## Task

The user asked for help without saying what they want. Look at the most recent part of the terminal first.

- If a command just failed, or there's an error in output or a log being viewed, explain the most likely cause and how to fix it.
- Otherwise, explain what's in the terminal: for example what source code does, what a diff changes, or what command output shows.

{{> _output-format}}
